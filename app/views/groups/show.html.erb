<!DOCTYPE html>
<html lang="ja">

    <head>
        <meta charset="UTF-8">
        <meta neme="viewport" content="width=device-width, initial-scale=1">
        <title>GD match</title>
        <link rel="stylesheet" href="../../assets/stylesheets/html5reset-1.6.1.css">

    </head>

    <body>
        <div align="center">
            <div class="group-show">
                <div class="show-group-chat-back">
                    <span class="group-show-group-chat-back">
                        <%= link_to groups_path do %>
                        <% end %>
                    </span>
                </div>
                <br>
                <div class="comment-wrapper">
                    <div>
                        <p><b>グループチャット</b></p>
                    </div>
                    <br>
                    <% @comments.each do |c| %>
                    <div>
                        <table>
                            <td><%= c.user.name unless c.user.blank? %></td>
                            <td>&nbsp;&nbsp;</td>
                            <td><%= c.content %></td>
                        </table>
                    </div>
                    <br>
                    <% end %>

                    <% if user_signed_in? %>
                    <%= form_with(model: [@group, @comment], local: true) do |f| %>
                    <%= f.text_area :content, size: "54x2" %><br>
                    <%= button_tag type: "submit" do %>
                    <i class="far fa-comments"></i> メッセージ送信
                    <% end %>
                    <% end %>
                    <% end %>
                </div>
                <br>
                <%= link_to "グループへ作成画面に戻る", groups_path %><br>
            </div>
            <br>
            <%= link_to "グループ画面へ", groups_path(@group.id) %><br>
            <br>
            <%= link_to "Tweet一覧に戻る", tweets_path %>

        </div>
    </body>

</html>